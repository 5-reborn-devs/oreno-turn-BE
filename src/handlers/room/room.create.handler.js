import { getProtoMessages } from '../../init/loadProto';

export const createRoomHandler = () => {
  const protoMessages = getProtoMessages();
  const roomData = protoMessages.common.roomData;

  return;
};
